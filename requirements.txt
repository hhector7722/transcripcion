# Dependencias mínimas para despliegue en Vercel (límite 500 MB Lambda)
# Solo lo que app.py necesita. NO incluye faster-whisper/torch (7+ GB).
fastapi
uvicorn
python-multipart
# Firebase (Storage + Firestore) para audios en la nube (móvil)
firebase-admin
